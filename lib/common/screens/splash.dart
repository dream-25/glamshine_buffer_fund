import 'package:flutter/material.dart';

class CommonSplash extends StatelessWidget {
  const CommonSplash({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
